# This log file is used to control logging when testing.
# In order tell the JVM to use this file when executing a junit test,
# configure the test's launch configuration with the following VM argument:
# -Djava.util.logging.config.file=test/logging.properties


# log to the console
handlers = java.util.logging.ConsoleHandler

# log levels: SEVERE, WARNING, INFO, CONFIG, FINE, FINER, FINEST

#configure the root logger
.level = INFO

com.shopstyle.snout.level = INFO
